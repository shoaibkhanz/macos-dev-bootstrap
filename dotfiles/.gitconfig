# Reference .gitconfig — NOT symlinked by install.sh.
# Configure user.name and user.email manually per machine:
#   git config --global user.name "Your Name"
#   git config --global user.email "you@example.com"

[init]
	defaultBranch = main
[core]
	excludesfile = ~/.gitignore_global
	editor = nvim
